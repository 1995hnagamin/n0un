language: c
sudo: required
script:
  - echo "yes"| sudo add-apt-repository ppa:avsm/ppa
  - sudo apt-get update
  - sudo apt-get install ocaml ocaml-native-compilers camlp4-extra opam omake
  - opam install ounit
  - omake test
env:
  - OCAML_VERSION=4.01
os:
  - linux
