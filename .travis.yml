language: c
sudo: required
script:
  - sudo apt-get install ocaml
  - wget https://raw.github.com/ocaml/opam/master/shell/opam_installer.sh
  - sh opam_installer.sh ~/local/bin
  - export PATH=$HOME/local/bin:$PATH
  - opam install ounit
  - omake test
env:
  - OCAML_VERSION=4.01
os:
  - linux
